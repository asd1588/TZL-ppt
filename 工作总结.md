# TZL PPT 定制网站 — 项目工作总结

## 一、项目概述

**项目名称**：TZL PPT 定制（TZL-ppt）
**技术栈**：React 18 + Vite 6 + JavaScript (JSX) + CSS3
**部署平台**：GitHub Pages
**项目定位**：一个面向 PPT 定制服务的品牌展示与作品集网站，包含首页 Hero、作品类型走马灯、主题风格展示、作品卡片墙、联系方式等模块。

---

## 二、搭建流程

### 2.1 初始化阶段

1. **选择技术栈**：React 18 + Vite 6（基于用户要求）
2. **创建项目**：`npm create vite@latest`，选择 React 模板
3. **安装依赖**：
   - `motion`（动画库，用于 3D 倾斜卡片效果）
   - `postprocessing` / `three`（用于 PixelBlast 像素背景特效）
   - `gh-pages`（部署工具）
4. **目录结构设计**：按功能模块拆分组件（Header / FolderStack / WorkTypes / ThemeShowcase / WorksGallery / Contact / Notification）

### 2.2 模块开发

每个模块独立开发，采用**自顶向下**的方式：

| 模块 | 功能 | 技术要点 |
|------|------|---------|
| Header | 导航栏 + 搜索 | 固定定位、毛玻璃效果、模糊搜索下拉 |
| Hero | 首页主视觉 | 双栏 Flex、文件夹堆叠轮播、地球/飞机动画 |
| FolderStack | 文件夹堆叠 | CSS 3D 变换、自动轮播、hover 暂停/展开 |
| WorkTypes | 作品类型走马灯 | requestAnimationFrame + transform 驱动、拖拽切换 |
| ThemeShowcase | 主题风格展示 | 12 套预设风格卡片、折叠/展开 |
| WorksGallery | 作品展示墙 | TiltedCard 3D 倾斜视差、聚焦/预览模式 |
| Contact | 联系方式 | 三卡布局、微信/咸鱼翻转、二维码展示 |
| PixelBlast | 像素背景 | Canvas 粒子特效 |

### 2.3 配置集中化

所有可编辑内容统一到 `src/data/siteConfig.js`：

```
BRAND → 品牌名
NAV → 导航菜单
HERO → 首页标题/描述
FOLDERS → 文件夹数据
WORK_TYPES → 作品类型
THEMES → 12 套主题风格
WORKS → 16 套作品
CONTACTS → 联系方式数据
CONTACT_CARDS → 卡片独立配置
SECTION_TITLES → 模块标题
```

### 2.4 部署流程

1. **手动部署**（第一阶段）：`vite build` → `gh-pages -d dist`
2. **CI/CD 自动化**（第二阶段）：GitHub Actions 工作流
3. **优化部署**（第三阶段）：`--base` 参数 + 后处理路径替换

---

## 三、踩过的坑

### 坑 1：GitHub Pages 路径问题

**问题**：GitHub Pages 部署在子路径 `/TZL-ppt/`，但代码中引用资源使用绝对路径 `/img/xxx.jpg`，导致 404。

**原因**：Vite 的 `base` 配置只影响 CSS 文件中 `url()` 的路径和 asset 加载，但不处理 JSX 内联样式的 `url(/img/...)` 和 JS 中字符串字面量 `"/ppt/xxx"`。

**解决**：
- 方案一：在组件代码中使用 `import.meta.env.BASE_URL` 拼接路径
- 方案二：构建后在 dist 文件中做 sed 替换（用于 GitHub Actions）
- 方案三：用 `--base=/TZL-ppt/` 构建，再用后处理修复内联路径
- **教训**：JSX 内联 `style={{ background: "url(/img/xxx.png)" }}` 不会被 Vite 的 base 转换处理，需要手动处理。

### 坑 2：PowerShell 编码破坏中文

**问题**：使用 PowerShell `@''@` 和 `-replace` 操作包含中文的文件时，UTF-8 编码被破坏，中文变成乱码（如 "定制" → "瀹氬埗"）。

**原因**：PowerShell 的字符串处理和管道操作会按系统默认编码（Windows-1252）重新编码，损坏多字节 UTF-8 字符。

**解决**：
- 使用 Node.js 的 `fs.readFileSync` 和 `fs.writeFileSync` 处理文件，确保 UTF-8 编码
- 或者用 `[System.IO.File]::ReadAllText/WriteAllText` 绕过 PowerShell 的编码转换
- 使用 `node -e` 执行 JavaScript 代替 PowerShell `-replace`
- **教训**：**永远不要用 PowerShell 的 `-replace` 操作包含非 ASCII 字符的中文文件**。改用 Node.js。

### 坑 3：gh-pages ENAMETOOLONG 错误

**问题**：`gh-pages -d dist` 报错 `spawn ENAMETOOLONG`。

**原因**：`dist/` 下有 4404 个文件（主要是 PPT 预览 HTML 和资源），globby 生成的 `git rm` 参数列表超过 Windows 命令行长度限制。

**解决**：改用手动 git 操作：
```bash
cd dist
git init
git checkout -b gh-pages
git add -A
git commit -m deploy
git push -f origin gh-pages
```

### 坑 4：Vite 缓存导致旧配置残留

**问题**：修改 `vite.config.js` 的 `base` 后重启 dev server，但资源仍按旧 base 路径加载。

**原因**：Vite 在 `node_modules/.vite/` 缓存了转换后的模块。

**解决**：删除 `node_modules/.vite/` 目录后再重启 dev server：
```bash
Remove-Item -Recurse -Force node_modules/.vite/
```

### 坑 5：CSS @keyframes 动画与 JS 动画冲突

**问题**：作品类型走马灯使用 `scrollLeft += 0.8` 驱动，但 CSS 中残留了 `@keyframes scrollLeft` 动画，导致冲突，走马灯卡死。

**原因**：CSS animation 的 transform 优先级高于 JS 设置的 transform，hooks 运行时两者相互覆盖。

**解决**：去掉 CSS 动画，改用纯 JS + `requestAnimationFrame` + `transform: translateX()` 驱动。

### 坑 6：事件监听与锚点滚动冲突

**问题**：导航栏"定制"按钮点击后滚动到联系方式模块，但页面滚动时导航高亮检测不准确。

**原因**：`scroll` 事件密集触发导致频繁计算，`getBoundingClientRect` 的 offset 值与实际滚动位置有偏差。

**解决**：使用 `requestAnimationFrame` 节流，`scroll-padding-top` 补偿固定导航栏高度。

### 坑 7：3D 翻转卡片与 hover 动画冲突

**问题**：微信卡片点击翻转 180° 时，鼠标 hover 的 `translateY(-6px)` 动画干扰翻转效果。

**原因**：CSS 动画优先级高于过渡，翻转后 hover 的 transform 覆盖了 flip 状态。

**解决**：翻转状态下禁用 hover 动画：`.flip-card.flipped{transform:none!important}`。

---

## 四、学到的知识

### 4.1 Vite 构建与部署

- **`base` 配置**：影响构建产物的资源路径前缀，不影响 JSX 内联 `url()`
- **`import.meta.env.BASE_URL`**：可以在代码中获取当前 base 路径，用于动态拼接资源 URL
- **`public/` 目录**：文件原样复制到构建产物根目录，按原始路径访问
- **`--base` 参数**：构建时可通过命令行指定，不修改配置文件

### 4.2 CSS 进阶

- **backdrop-filter**：毛玻璃效果（`blur(18px) saturate(1.3)`）
- **perspective + preserve-3d**：3D 翻转卡片实现
- **transform-origin**：控制变换原点（文件夹以左下角为圆心旋转）
- **cubic-bezier** 弹性曲线：弹起动画更自然
- **overflow-x: auto + scrollbar-width: none**：隐藏滚动条的横向滚动容器
- **@support / -webkit-backdrop-filter**：跨浏览器兼容

### 4.3 React 模式

- **forwardRef**：用于锚点滚动
- **useCallback + useRef**：避免不必要的重渲染
- **自定义 hooks（useCarousel）**：轮播逻辑复用
- **dangerouslySetInnerHTML**：动态注入 SVG
- **条件渲染 + key**：翻转卡片前后切换

### 4.4 性能优化

- **requestAnimationFrame**：替代 setInterval 实现平滑动画
- **transform 替代 scrollLeft**：避免浏览器重排
- **loading="lazy"**：图片懒加载
- **CSS will-change**：提前告知浏览器元素将变化

### 4.5 编码与兼容性

- **UTF-8 BOM 问题**：PowerShell 默认在 UTF-8 文件头加 BOM，导致 Vite/Babel 解析报错
- **Windows CRLF vs LF**：Git 在 Windows 上自动转换行尾符
- **Emoji 在 PowerShell 中的编码损坏**：避免用 PowerShell 处理含 emoji 的文件

---

## 五、备份与部署自动化

### 5.1 Git 备份

```bash
git add -A
git commit -m "描述性提交信息"
git push
```

每次 `git push` 到 `main` 分支即完成备份。

### 5.2 GitHub Actions 自动部署

工作流文件 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm ci
      - run: npx vite build --base=/TZL-ppt/
      - run: |
          # 修复内联路径
          sed -i 's|/img/|/TZL-ppt/img/|g' dist/assets/*.js dist/assets/*.css
          sed -i 's|"/ppt/|"/TZL-ppt/ppt/|g' dist/assets/*.js
      - uses: peaceiris/actions-gh-pages@v3
        with:
          publish_dir: ./dist
```

**工作流程**：
1. 推送代码到 `main` 分支
2. GitHub Actions 自动触发
3. 安装依赖 → 构建（带 base 路径）→ 修复路径 → 部署到 `gh-pages`
4. 1-2 分钟后访问 `https://asd1588.github.io/TZL-ppt/` 查看更新

### 5.3 手动部署命令

```bash
# 构建
npx vite build --base=/TZL-ppt/

# 手动推送 dist 到 gh-pages 分支
cd dist
git init && git checkout -b gh-pages
git add -A && git commit -m deploy
git remote add origin https://github.com/asd1588/TZL-ppt.git
git push -f origin gh-pages
```

---

## 六、技术要点总结

| 类别 | 要点 |
|------|------|
| 框架 | React 18 + Vite 6 + motion |
| 动画 | CSS 3D transform + requestAnimationFrame |
| 交互 | 文件夹轮播 / 卡片翻转 / 走马灯拖拽 |
| 响应式 | 汉堡菜单 + 网格自适应 |
| 部署 | GitHub Actions + gh-pages 分支 |
| 配置 | 集中式 siteConfig.js |
| 编码 | UTF-8 全程优先，避免 PowerShell 处理中文 |
| 构建 | `--base` 参数 + 后处理路径替换 |
