# TZL 项目工作流（效率版）

## 一、项目初始化

```bash
npm create vite@latest . -- --template react
npm install motion postprocessing three
npm install -D gh-pages
```

目录结构：
```
src/components/模块名/模块名.jsx + 模块名.css
src/data/siteConfig.js    ← 所有文字/图片/链接集中于此
src/data/constants.js     ← 重导出 siteConfig
.github/workflows/deploy.yml
```

## 二、开发流程

### 2.1 写新模块
1. `src/data/siteConfig.js` 加数据
2. `src/components/模块名/模块名.jsx` 写组件
3. `src/App.css` 写样式（不拆分 CSS，方便管理）
4. `src/App.jsx` 引入组件

### 2.2 改文案/图片
**只改 `siteConfig.js`**，不碰任何组件。

### 2.3 提交流程
```bash
git add -A
git commit -m "类型: 描述"
git push                    # ≈ 自动备份
```

---

## 三、铁律（踩坑总结）

### 🔴 红线（绝对禁止）

| # | 禁止事项 | 替代方案 |
|---|---------|---------|
| 1 | ❌ PowerShell 的 `-replace` 处理含中文的文件 | ✅ 用 `node -e` 或 `[System.IO.File]::WriteAllText` |
| 2 | ❌ PowerShell 的 `@''@` 写中文到 JSX 文件 | ✅ 用 Node.js 的 `fs.writeFileSync` + `'utf8'` |
| 3 | ❌ 直接修改 `public/` 里的图片路径 | ✅ 放 `public/img/`，代码中用 `/img/xxx.jpg` |
| 4 | ❌ 内联 `style={{ background: "url(/img/x.png)" }}` 不加 base 处理 | ✅ 部署时用 sed 替换或 `import.meta.env.BASE_URL` |

### 🟡 警告（必须注意）

| # | 注意事项 | 说明 |
|---|---------|------|
| 1 | `vite.config.js` 的 `base` 改了后**必须删除** `node_modules/.vite/` 再重启 | 否则旧缓存残留 |
| 2 | JSX 内联 `url()` ⚠️ **不会被 Vite 的 base 转换** | 需要手动处理或构建后 sed |
| 3 | gh-pages 在 dist > 4000 文件时会 `ENAMETOOLONG` | 改用手动 git 操作 |
| 4 | `Set-Content` 默认加 BOM → Vite 解析报错 | 加 `-Encoding utf8NoBOM` 或 Node.js 写文件 |
| 5 | CSS animation 的 transform 优先级 > JS 的 transform | 不要混用两者控制同一个属性 |

---

## 四、部署工作流

### 4.1 自动部署（推荐）
- 提交 `.github/workflows/deploy.yml` 后，每次 `git push` 自动部署
- 无需手动操作，等 1-2 分钟访问 Pages URL

### 4.2 手动部署（紧急）
```bash
npx vite build --base=/TZL-ppt/

# 修复内联路径（Windows 用 Node.js）
node -e "
const fs=require('fs');
const dir='dist/assets';
fs.readdirSync(dir).filter(f=>f.endsWith('.js')).forEach(f=>{
  let c=fs.readFileSync(dir+'/'+f,'utf8');
  c=c.replace(/\/img\//g,'/TZL-ppt/img/');
  c=c.replace(/\"\/ppt\//g,'\"/TZL-ppt/ppt/');
  fs.writeFileSync(dir+'/'+f,'utf8');
});"

# 推送 dist 到 gh-pages
cd dist
git init && git checkout -b gh-pages
git add -A && git commit -m deploy
git remote add origin https://github.com/asd1588/TZL-ppt.git
git push -f origin gh-pages
```

---

## 五、快速诊断清单

遇到问题先按顺序排查：

| 步骤 | 检查项 | 命令/方法 |
|------|-------|----------|
| 1 | 有编译报错？ | 看终端输出，定位文件-行号 |
| 2 | 图片 404？ | 浏览器 F12 → 网络 → 看请求 URL 是否有 base 前缀 |
| 3 | 中文乱码？ | `node -e "console.log(fs.readFileSync('文件路径','utf8').charCodeAt(0).toString(16))"` 看首字节是不是 EF BB BF |
| 4 | 配置不生效？ | 删 `node_modules/.vite/` 重启 |
| 5 | 按钮样式不对？ | 检查 `className` 比较是否匹配（编码损坏导致不匹配） |
| 6 | gh-pages 错误？ | 检查 dist 文件数（超过 3000 用手动部署） |

---

## 六、核心命令速查

```bash
# 开发
npm run dev

# 构建
npx vite build --base=/TZL-ppt/

# 部署
npx gh-pages -d dist

# Git
git add -A && git commit -m "msg" && git push

# 重置本地代码（保留备份）
git checkout HEAD -- src/

# 清 Vite 缓存
Remove-Item -Recurse -Force node_modules/.vite/
```
